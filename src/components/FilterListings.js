import React, { Component } from 'react'

class FilterListing extends Component {
  render () {
    return (
      // TODO: Create a filter method that allows the user to filter through the
      // listings. Must include a text input or dropdown.
    )
  }
}

export default FilterListing
